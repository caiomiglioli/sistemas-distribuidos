#install
sudo apt update
sudo apt install docker docker-compose
docker run --name rabbitmq_sd -p 5672:5672 rabbitmq

#start
docker ps -a #pegar o codigo do container
docker start <container-id>

#delete
docker ps -a #pegar o codigo do container
docker rm <container-id>